Java uses references, not raw pointers; the pointer line shows System.identityHashCode as a hex address proxy (value varies per run).

___

##### Run Command:
`java pointers.java`

##### Results:
`initial: 1`
`zeroval: 1`
`zeroptr: 0`
`pointer: 0x7364985f`
