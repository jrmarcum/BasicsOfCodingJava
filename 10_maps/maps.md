Java's `LinkedHashMap.toString()` uses `{k=v}` format vs Go's `map[k:v]`; insertion order is preserved.

___

##### Run Command:
`java maps.java`

##### Results:
`map: {k1=7, k2=13}`
`v1:  7`
`len: 2`
`map: {k1=7}`
`prs: false`
`map: {foo=1, bar=2}`
