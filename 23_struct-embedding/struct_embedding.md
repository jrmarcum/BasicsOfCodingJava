Java has no struct embedding; composition is used with a Base field on Container, and the Describer interface is implemented by delegation.

___

##### Run Command:
`java struct_embedding.java`

##### Results:
`co={num: 1, str: some name}`
`also num: 1`
`describe: base with num=1`
`describer: base with num=1`
