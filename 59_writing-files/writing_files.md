Requires a `tmp/` directory in the lesson directory before running; creates `tmp/dat1.txt` and `tmp/dat2.txt`.

___

##### Run Command:
`java writing_files.java`

##### Results:
`wrote 5 bytes`
`wrote 7 bytes`
`wrote 9 bytes`
