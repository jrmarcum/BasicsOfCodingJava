Java uses try-finally to ensure cleanup runs after a block, equivalent to Go's defer. Requires a tmp/ directory to exist at runtime.

___

##### Run Command:
`java defer.java`

##### Results:
`creating`
`writing`
`closing`
