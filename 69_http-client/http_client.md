Requires network access; response body lines vary. Java reports status as integer only (`200 OK` constructed manually).

___

##### Run Command:
`java http_client.java`

##### Results:
`Response status: 200 OK`
`<!DOCTYPE html>`
`<html lang="en">`
`  <head>`
`    <meta charset="utf-8">`
`    <title>Go by Example</title>`
