Blocks until Ctrl+C; Java shutdown hook prints the exit message (no signal name like Go's `interrupt signal received`).

___

##### Run Command:
`java signals.java`

##### Results:
`awaiting signal`
`^C`
`exiting`
